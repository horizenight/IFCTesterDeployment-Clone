services:
  # A Docker web service at Render
  - type: web
    name: IFCTester Deployment
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Webapp.Backend.App.main:app